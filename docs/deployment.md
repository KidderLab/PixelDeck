# Deployment

For a single-user workstation, run the Next.js server and worker as separate long-lived processes with storage on local SSD. For future multi-user deployments, move to PostgreSQL and shared media storage.
