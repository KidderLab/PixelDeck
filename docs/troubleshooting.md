# Troubleshooting

- Confirm `ffmpeg` and `ffprobe` are installed.
- Confirm the worker is running when imports or exports are queued.
- Check `storage/logs` for application and worker errors.
- Rebuild the FTS index if search results appear stale.
